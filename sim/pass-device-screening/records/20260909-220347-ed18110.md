# Record 20260909-220347-ed18110

- **Experiment**: pass-device-screening
- **Claim**: sg13_hv_pmos (Mpass's flavor, design/README.md "Pass
  device") screened at the dropout test point (Vin=2.10V,
  Vout=1.80V, gate=0V) for Vth, Ron*W and Cgate, and at the
  continuous-short current-limit condition (Vout=0V, Vin=3.63V,
  gate=0V) for terminal voltage/current stress -- the input to
  spec/porting-plan.md Sec 4 item 1.
- **Devices**: sg13_hv_pmos (PSP103.6 via psp103.osdi),
  W/L = {1um x 0.4/0.5/1.0um (Ron*W-normalized), 300um x 0.5um
  (design/ldo_core.sch's as-drawn Mpass size, a direct cross-check)},
  ng=1, m=1.
- **PDK**: `ihp-sg13g2` at `/Users/rwalters/share/pdk` -- pinned release: see
  `sim/pdk.json` (IHP-Open-PDK v0.3.0).
- **OSDI models**: `/Users/rwalters/share/pdk/ihp-sg13g2/libs.tech/ngspice/osdi` -- built by
  `sim/tools/build-osdi.sh`.
- **ngspice**: `** ngspice-46 : Circuit level simulation program`
- **Corner matrix run**: device {w1u_l0.4u, w1u_l0.5u, w1u_l1.0u, w300u_l0.5u} x process
  {tt ss ff sf fs} (cornerMOShv.lib, space-separated above) x
  temperature {-40 27 125}C = 120 points (both benches
  combined: dropout + continuous-short-stress). The supply-voltage
  axis is NOT independently swept -- both test points are fixed
  absolute Vin values by definition (Vin=2.10V dropout,
  Vin=3.63V=Input-row-max stress), not relative to a swept nominal
  supply corner. See README.md "Corner grid and axes swept".
- **Result**: 120/120 points PASS (ngspice exit 0, all
  expected outputs present).
- **Completeness matrix**: OK -- every (device,corner,temp) cell has exactly 1 row
- **Sanity checks** (see `corners/20260909-220347-ed18110/sanity_checks.txt`):
  - INFO binding dropout corner (worst Ron*W, w1u_l0.5u): ss/125C Ron*W=13013.9 ohm.um
  - INFO binding stress corner (worst Id/um, w1u_l0.5u): ff/-40C Id/um=3.180170e-04 A/um
  - PASS Vsg at continuous-short stress (|Vsg|=3.63V, bias-imposed) exceeds the PDK's stated 3.3V max VGS rating at 60/60 points (expected: all, since Vsg is set purely by Vin_max/gate bias, not corner-dependent).
- **Netlist provenance**: bare-device deck (this experiment
  instantiates sg13_hv_pmos directly, per
  `testbench/tb_dropout_pmos.spice.tmpl` /
  `testbench/tb_stress_pmos.spice.tmpl`), cross-referenced against
  `design/ldo_core.sch`'s Mpass topology (common-source, source to
  VIN, drain to VOUT, body to VIN) at the design's own repo git sha
  `ed18110` -- not extracted from or instantiating
  `design/netlist/ldo_core.spice` directly, since this is a
  device-characterization study, not a circuit-level bench (same
  posture as sim/gm-id-characterization in the sibling
  sg13g2-opamp repo).
- **Links**:
  - Templates: `testbench/tb_dropout_pmos.spice.tmpl`,
    `testbench/tb_stress_pmos.spice.tmpl`
  - Per-point generated netlists: `netlist-snapshots/20260909-220347-ed18110/`
  - Per-point raw ngspice logs + per-point DC sweep CSVs:
    `corners/20260909-220347-ed18110/`
  - Parsed, merged CSV (all points, all derived quantities):
    `records/20260909-220347-ed18110.csv`
- **Timestamp / author**: 2026-09-09T22:07:56Z, Loom Builder
  (agent), issue #13.
